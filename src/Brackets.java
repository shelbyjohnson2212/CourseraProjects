import java.util.Stack;

public class Brackets {

	//create stack for pairs of brackets
	IsBalanced(str)//
	
	Stack<E> stack;
	for char in str;
		if char in ['(, '[''];
			stack.Push(char);
			else:
				if stack.Empty();
					retrn False;
					top <- stack.Pop();
					if (top = '[' and char != ']') or
						(top = '(' and char != ')'):
							return False;
							return stack.empty();
				
	
	
}
 